name: Java Autograding Directo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

     - name: Investigar carpetas
        run: |
          echo "--- DONDE ESTOY ---"
          pwd
          echo "--- QUE HAY EN LA RAIZ ---"
          ls -R
